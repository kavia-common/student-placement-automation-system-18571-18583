# PUBLIC INTERFACE: Environment configuration for placement_frontend
# Backend base URL (do not include trailing slash)
REACT_APP_API_BASE_URL=http://localhost:8000

# Site URL used for email redirects (if backend requires)
REACT_APP_SITE_URL=http://localhost:3000
